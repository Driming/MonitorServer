<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hc.entity.control.role.RoleMapper" >
  <resultMap id="BaseResultMap" type="com.hc.entity.control.role.Role" >
    <id column="rid" property="rid" jdbcType="INTEGER" />
    <result column="name" property="name" jdbcType="VARCHAR" />
  </resultMap>
  <sql id="Base_Column_List" >
    rid, name
  </sql>
  
  <select id="selectAll" resultMap="BaseResultMap">
    select 
    <include refid="Base_Column_List" />
    from control_role
  </select>
  
  <delete id="deleteRole" parameterType="java.lang.Integer" >
    delete from control_role
    where rid = #{rid,jdbcType=INTEGER}
  </delete>
  
  <insert id="insertRoleSelective" parameterType="com.hc.entity.control.role.Role" >
    insert into control_role
    <trim prefix="(" suffix=")" suffixOverrides="," >
      <if test="rid != null" >
        rid,
      </if>
      <if test="name != null" >
        name,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides="," >
      <if test="rid != null" >
        #{rid,jdbcType=INTEGER},
      </if>
      <if test="name != null" >
        #{name,jdbcType=VARCHAR},
      </if>
    </trim>
  </insert>
  
  <update id="updateRoleSelective" parameterType="com.hc.entity.control.role.Role" >
    update control_role
    <set >
      <if test="name != null" >
        name = #{name,jdbcType=VARCHAR},
      </if>
    </set>
    where rid = #{rid,jdbcType=INTEGER}
  </update>
  
</mapper>